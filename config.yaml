# DVR Import Configuration
username: daniel
sd_card_name: DVR_SD

# Default output directory (can be overridden at runtime)
default_output_dir: ~/DVR-Recordings

# Operation mode: "copy" or "move"
default_mode: move

# GUI settings
gui:
  checksum_enabled: true
  first_run_completed: false
